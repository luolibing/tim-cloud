server:
  port: 8004

eureka:
  client:
    healthcheck:
      enabled: true
    service-url:
      defaultZone: http://peer1:8761/eureka/

  instance:
    metadata-map:
      management.port: ${management.port:7071}